name: libasm

on:
  push:
    branches: [ develop/libc ]
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install ASM dependencies
        run: sudo apt-get install nasm

      - name: Builds static library libasm.a
        run: make
    
